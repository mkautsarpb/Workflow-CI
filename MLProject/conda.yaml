name: heart-disease-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - scikit-learn
      - mlflow
      - dagshub
      - joblib
      - matplotlib
      - seaborn
